# Code review — P9-10-verify · engineer revision 1

## Verdict: APPROVED

## Findings
| id | severity | file:line | issue | required change |
|----|----------|-----------|-------|-----------------|
| C1 | nit | dev-board/code-review/P9-10-verify/engineer.md:64-70 | The "formatting-only" claim on the 17 sibling files cannot be literally proven by a hunk diff: the entire P9 phase is uncommitted and every reformatted file is either untracked (brand-new, no baseline) or `M` vs HEAD=P8 (its diff is full sibling-task feature work, not an isolable format hunk). So no pre-format baseline exists to diff against. | None required — the claim is sound by construction (see Notes). Optional: next time, run `ruff format` as its own step *before* authoring so each file's format state is committed independently. |
| C2 | nit | tests/test_p9_exit_verification.py:249-563 | Points 1–6 skip cleanly when Postgres is unreachable, so in a DB-less CI they contribute no signal (only 7–8 always run). This is the documented, task-sanctioned posture, not a defect. | None — acceptable; the live-DB requirement is inherent to proving shared-table composition (matches the `*_persistence` precedent). |

## Notes
- **Deliverable is well-composed and on-scope.** `test_p9_exit_verification.py` genuinely exercises the *chain*, not re-mocked pieces: point 1 writes via real `run_learn_from_turn` then reads via the compiled `stream_graph` recall over the **same** `provider`; point 3 deletes via `DELETE /api/memory/{id}` and re-asserts the row is gone from both `GET` and `store.search_memories` (proves one table, not two paths); points 4–5 assert PII redaction / Art. 9 drop through the real gate via the API response; point 6 exercises the real stale-user finder with a capturing eraser (no destructive delete on the shared DB); points 7–8 are source-scan contract checks. Fakes are confined to the true external edges (embedder, extractor LLM). Fixtures clean up (user-delete cascade) in `finally`.
- **Format-only claim — confirmed to the extent the uncommitted tree allows.** `ruff format` (v0.15.20, the pinned CI version) is a deterministic, semantics-preserving formatter; independently I re-ran and got `ruff format --check .` → **266 files already formatted** and `ruff format --diff .` → **empty** (idempotent, tree fully canonical). `ruff check .` → **All checks passed!**. A smuggled non-whitespace edit would have to survive a green `ruff check` + the reported `mypy` (158 files) + 959-test run. Combined, that is adequate assurance the pass was formatting-only. A literal per-file format hunk-diff is not reconstructable because no P9 file has a committed baseline — recorded as C1, not a gate.
- **Independently verified locally:** `ruff --version` = 0.15.20; `ruff check .` green; `ruff format --check/--diff` clean; the two always-run tests (`test_p9_frontend_clients_target_the_backend_endpoints`, `test_p9_pii_gate_and_memory_primitives_are_not_duplicated`) → **2 passed**. The live-Postgres points (1–6) and the full 959-test run I did not re-run here (heavy live infra); I relied on the engineer's pasted results for those, corroborated by the mypy typing fix being the real-node-alias idiom (`PlannerNode`/`NodeUpdate`) already used by sibling agent suites.
- **Process note (not a gate):** a verification-only task touched 17 sibling files. Disclosed and formatting-only, and since the whole phase commits together this is harmless — but it does mean those siblings' final bytes carry a change made outside their own review. Fine here.
- **Cross-task consistency (point 8) holds** — `special_category_of`/`gate_candidate`/`_near_duplicate`/`_clamp_confidence`/`list_user_memories` each resolve to a single home and guest paths import rather than re-implement. No drift to route back to an owning task; no phase-exit gap found.
