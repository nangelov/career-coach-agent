# Code review — P11-04-observability-verify · engineer revision 1

## Verdict: APPROVED

## Findings
| id | severity | file:line | issue | required change |
|----|----------|-----------|-------|-----------------|
| C1 | nit | engineer.md §Evidence-1 (`http.target: Str(/api/chat)`) | The captured HTTP span shows `http.target` passed through un-redacted. Harmless for `/api/chat` (no secret in path/query), but the known P11-01 OTel gap is auto-instrumentation leaking `?code=`/`?state=`/`?token=` on OAuth-callback URLs (scheme-less, so the contact-redactor misses it). This verification did not exercise a query-bearing endpoint, so it neither confirms nor refutes that gap. | Out of scope for this exit gate; flag for the owner runbook / P11-01 follow-up that a callback-shaped URL should be driven through the collector once, to prove `http.target`/`url.query` redaction. No change required here. |

## Notes
- **Evidence substantiates the claims.** Independently verified: the graph node names in the span dump (`input_guardrail`, `memory_recall`, `planner`, `market_intel`, `responder`, `output_guardrail`, `memory_writer`) are the real constants in `backend/app/agents/graph.py`; `is_worker=True` is set only on RAG/WEB_SEARCH/MARKET_INTEL, which corroborates the "`graph.node.planner` carries no `worker.name`" claim (P11-01 C2 fix holds). The requirement "planner + ≥1 worker + responder under one trace" is genuinely met (`market_intel` is a real worker doing a real Postgres read).
- **Real docker-compose run, honestly caveated.** The LLM-transport stub swap (HF egress DNS-blocked in this sandbox) is disclosed, kept in scratchpad, and not committed. It does not weaken the verification: span redaction is independent of the LLM, and the synthetic PII markers were carried in the message/CV, not the model output. The PII grep is thorough (specific markers + email/phone/token regexes across 6490 lines, all zero; 68 `[URL REDACTED]` confirming redaction is actually firing rather than vacuously absent). Celery producer+consumer spans captured on both `career-coach-backend` and `career-coach-worker`. Stack torn down; `db`/`redis` left as found.
- **Sentry / GA4 correctly scoped.** Sentry inert-when-unset and delivery-attempt-with-dummy-DSN both demonstrated, with the PII-safe init options confirmed live; "no PII reaches Sentry" correctly cited to P11-02's `before_send` tests rather than re-derived. GA4 "loads + fires" is covered by the new `frontend/__tests__/analytics.test.ts` (verified: asserts `gtag('event', <name>)` for `send_message`/`upload_cv`/`generate_pdp` and that free-text params are stripped); content-safety cited to P11-03. The 3-item owner runbook cleanly separates locally-verifiable from account-dependent confirmations.
- **Diff is clean for this task.** Only new file attributable here is the frontend GA4 test; the other uncommitted P11 tree (graph.py, observability/, docker-compose.otel.yml, etc.) belongs to P11-01/02/03. No product code touched, no secrets, no test weakened (`npx jest` 224 passed; backend observability suites 27 passed).
