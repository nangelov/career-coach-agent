# Code review — P6-09-manual-verify · engineer revision 1

## Verdict: APPROVED

## Findings
| id | severity | file:line | issue | required change |
|----|----------|-----------|-------|-----------------|
| C1 | minor (note, not gated) | `app/agents/market_agent.py:439-465` (`_resolve_baseline`) | Real defect the engineer flagged **is real** and correctly reproduced: `shared_kb_document_ids(source_types=["curated"])` returns taxonomy occupations **and** mined role-profile summaries (`ROLE_PROFILE_SOURCE_TYPE = "curated"`, line 103, title `"Market requirements: <role>"`) **and** learning resources — all `source_type="curated"`, distinguished only by `meta.kind`. `_resolve_baseline` filters by `source_type` alone and takes the top hybrid-search hit (`k=1`, no similarity threshold), so read-time canonicalization can drift to a summary/other-curated title → `get_role_profile` miss → perpetual `202` re-enqueue loop. Not a P6-09 blocker (see Notes); **route to P6-04**: restrict the taxonomy match by `meta.kind` (occupation) rather than `source_type` alone. | Route a P6-04 fix; do not gate P6-09. |
| C2 | nit | `tests/test_p6_exit_verification.py:294-323` | Cache-reuse proof (point 3) drives the real endpoint but injects a stubbed `_SpyResolver`, so it does **not** exercise the real `_resolve_baseline`; the live round-trip (`test_live_mining_then_requirements_then_gap:621-624`) has a guard that **skips** exactly when canonicalization drifts. Neither test exercises the C1 drift path — the "criterion met" conclusion is conditional on the ranking holding, not proven robust. Acceptable given verification-only scope; note the caveat. | None required; caveat noted for the orchestrator. |

## Notes
- **Is the flagged P6-04 observation blocking or fairly deferred?** Fairly deferred for P6-09; it is a genuine latent defect that must be routed to **P6-04**. Rationale: (1) P6-09 is verification-only and changed **no** product code — gating it would force a P6-04 fix inside a verify task, violating the task's explicit "flag precisely, route to the owning task, don't patch around" instruction; (2) the exit criterion holds under the documented production condition (P6-01 seeds the taxonomy at deploy), which the engineer verified empirically against live Postgres; (3) the engineer surfaced it precisely, with impact analysis and a concrete fix, exactly as asked. Verified independently: role-profile summaries and learning resources do share `source_type="curated"` with taxonomy occupations, so the ambiguity is exactly as described.
- **Test quality:** discriminating where it matters. Off-topic short-circuit asserts `responder.stream_messages == []` **and** `complete_messages == []` plus a `_BoomDB` that raises if any worker touches the DB (point 4). No-listings backend asserts an exact set `{"/api/jobs/status/{task_id}"}` (point 5). Cache-reuse asserts `db.sessions == 1`, `resolver.calls == 1`, `enqueuer.calls == []` (point 3). Cold-role asserts `202` + exactly one enqueue, never inline. Real graph/service/repo stack driven; only true external edges faked (extractor LLM, Tavily, crawl transport, embedder) — matches the P4-10/P5-08 posture.
- **Gates reproduced locally (all green):** `ruff check` → All checks passed; `ruff format --check` → 203 files already formatted (drift cleanup confirmed); `mypy app/ migrations/` → Success, 122 files; `pytest tests/test_p6_exit_verification.py -q` → **8 passed, 2 skipped** (the 2 live-DB proofs skip cleanly with no reachable Postgres, per convention — matches the engineer's report). Did not run the full 606-test offline suite or the frontend suite here; no reason to doubt the reported numbers given the isolated module + gates pass.
- **Reformat claim:** the 6 "reformatted" files are all part of the uncommitted P6 work (no committed baseline to diff against), and `ruff format --check` is clean — no logic risk. No product code changed by this task.
- **Security/scope:** verification-only; no secrets, no new external surface. Point 5 (no job-listings surface) independently re-confirmed via the OpenAPI path set assertion.
