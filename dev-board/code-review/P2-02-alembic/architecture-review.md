# Architecture review — P2-02-alembic · engineer revision 1

## Verdict: APPROVED

## Design conformance
| id | area | expected (design ref) | observed | gap / required change |
|----|------|-----------------------|----------|-----------------------|
| A1 | §8 target structure | Migration env under `backend/migrations/` | `env.py`, `versions/`, `script.py.mako`, `README`, `init/` all under `backend/migrations/`; `alembic.ini` at `backend/` (deliberate, documented — keeps `backend/` as the single cwd where `app/` is importable) | none — placement matches §8; ini-at-root is a sound, documented choice |
| A2 | §4 single source of connection config | Alembic reads `DATABASE_URL` from the app's `Settings`, no duplicated/hardcoded string | `env.py` imports `app.config.settings` and `config.set_main_option("sqlalchemy.url", settings.DATABASE_URL)`; `alembic.ini` deliberately omits `sqlalchemy.url` | none — app and Alembic cannot drift |
| A3 | §4 async Postgres | Async SQLAlchemy engine (asyncpg driver) | `run_async_migrations` uses `async_engine_from_config` + `NullPool` + `asyncio.run`; `DATABASE_URL` default DSN is `postgresql+asyncpg://…` | none |
| A4 | §4 shared `Base` / autogenerate target | `target_metadata = Base.metadata` from `app.repositories.postgres` (the one base all P2-03/04/05 models subclass) | Exactly that; `Base` is the P2-01 `DeclarativeBase` | none — `--autogenerate` will work the moment models land |
| A5 | §4 pgvector boundary (P0-07) | Extension bootstrapped once by `init/01_enable_pgvector.sql`; Alembic must not re-`CREATE EXTENSION` | Init SQL + docker-compose mount untouched (git clean); baseline migration is a documented no-op; `upgrade --sql` emits only `alembic_version` DDL, no `CREATE EXTENSION` (verified) | none |
| A6 | §4 schema mgmt distinct from runtime pool | Migrations not auto-run in FastAPI lifespan | `app/main.py` has no alembic/migrate hook; migrations reachable only via `Makefile` / explicit `alembic` / `docker compose run` | none — matches P2-01's app-owned pool vs. explicit schema step |
| A7 | Phase fit (P2 plumbing only) | Prove plumbing; no application tables yet | No-op baseline `0001` establishes the revision chain / `alembic_version` only; tables deferred to later migrations | none — no premature coupling to later phases |

## Cross-cutting checks
- [x] Fits target structure (§8) + layering (Router→Service→Agent/Repo): migration env sits beside the repository layer and targets its `Base`; no service/router touched.
- [x] Honors locked decisions: Postgres (pgvector) + Redis only — Alembic runs against Postgres, no MongoDB; self-hosted `pgvector/pgvector:pg16`; no ReAct/SSO/embedding surface in scope.
- [x] Interfaces-before-implementations: N/A (infra); correctly reuses the P2-01 `Base`/`Settings` seams rather than introducing a parallel config path.
- [x] Budget posture respected: OSS Alembic, self-hosted Postgres, no managed tier, no paid dependency.

## Notes
- Live-DB `upgrade head` → `downgrade base` verified against the running `pgvector/pgvector:pg16` container per `engineer.md`; `vector` extension confirmed present from P0-07 and not created by Alembic. Accepted as sufficient functional proof for infra plumbing.
- Cosmetic only (no action required): the baseline docstring numbers upcoming table migrations as users→P2-03 / kb→P2-04 / dashboard→P2-05, while `postgres.py`'s docstring says P2-02..04. Purely a comment drift; does not affect the revision chain or schema. Worth aligning opportunistically when P2-03 lands.
- `script.py.mako` is pre-modernized for the repo's ruff `UP` rules (`str | Sequence[str] | None`, `collections.abc.Sequence`) so future autogenerated migrations lint cleanly — a good forward-looking touch that keeps later phases friction-free.
