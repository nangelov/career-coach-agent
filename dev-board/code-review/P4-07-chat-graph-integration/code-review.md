# Code review — P4-07-chat-graph-integration · engineer revision 1

## Verdict: APPROVED

## Findings

| id | severity | file:line | issue | required change |
|----|----------|-----------|-------|-----------------|
| C1 | minor | app/agents/graph.py:439-445 | The two-phase split runs the **whole** pre-graph in `plan()` — including `output_guardrail_node` and the terminal `memory_writer_node` — over the *deterministic placeholder* response from `responder_node`, then streams the **real** `Responder` separately in `stream_response()`. The real, user-facing answer therefore never passes through the output-guardrail or memory-writer tail. Behaviorally correct today (both are allow-all/no-op stubs), but the engineer's claim that "P9/P10 replace those node bodies later without changing this integration" is optimistic: a real output guardrail (P10) would be inspecting placeholder text, and a real memory writer (P9) would never see the streamed answer. | No change required for P4 (stubs). Flag for P9/P10: the output-guardrail + memory-writer will need to move onto the streamed answer (post-stream) rather than the pre-graph tail — this integration *will* need rework then. Defer to the system-architect for the graph-fidelity call. |
| C2 | minor | app/services/chat.py:216-244 | Cancel is polled before the graph run and after planning, but **not during** the `plan()` phase itself — which now runs the real workers (RAG pgvector query, web-search crawl). A cancel arriving while a slow worker is executing is not observed until `plan()` returns. | Accept as a bounded latency tradeoff (matches the "checkpoint" model the task prescribes) — note only. If worker latency grows, consider a cancel-aware wrapper around `plan()` in a later task. |
| C3 | minor | app/services/chat.py:262-268 | Resilience asymmetry between the two stores on the success path: `self._memory.append(...)` (Redis) runs *before* the `DoneEvent` yield and does **not** swallow errors, while `_persist_turn` (Postgres) runs *after* the terminal event and swallows. A Redis hiccup after a fully-streamed answer converts a complete turn into a terminal `error` event (tokens already delivered, then error). | Consider moving `memory.append` after the `DoneEvent` yield (mirroring the persist-after-terminal posture) or wrapping it best-effort, so a working-memory write failure cannot error a turn whose answer already streamed. Non-blocking. |
| C4 | minor | app/schemas/chat.py:77-81; app/services/chat.py:196,345-346 | `ChatRequest.history` (client-supplied prior turns) still seeds this turn verbatim and now flows into the multi-agent graph — into the responder's `_build_messages` (and any planner history use). A client can inject `role="system"`/`role="tool"` messages to steer the responder. Self-scoped (own session), so impact is bounded, but the escape hatch now reaches more of the graph. | Carry forward to P10 guardrails: restrict client-supplied history to `user`/`assistant` roles (or drop the field). Not gating — same self-scoped surface already noted in P1. |
| C5 | nit | app/services/chat.py:345-347 | When client `history` is supplied it is returned verbatim and never reconciled with server-side `SessionMemory`; a subsequent `memory.append` mixes it with whatever is (or isn't) in Redis, so client history and server memory can diverge under the same `session_id`. Documented interim behavior. | No action for P4 — note so P1-05's Redis session store owners don't inherit ambiguity. |

## Notes

- **Verification run locally (from `backend/`):** `ruff check .` → all passed; `mypy app/ tests/` → 1 error, `tests/test_llm_router.py:309 Unused "type: ignore"`, confirmed **pre-existing and out of scope** (file untouched by this task per `git status`); `pytest -q` → **286 passed, 43 skipped** (skips are the live-Postgres integration suites). Matches the engineer report exactly.
- **Cross-cutting behavior genuinely preserved (not just claimed), confirmed against the adapted tests:**
  - **Cancel** — `test_chat_cancel.py` still asserts the three checkpoints (pre-graph, post-plan, mid-stream), partial-answer persistence, flag-clear, session isolation, stale-flag clear, and 202/403/401 on the endpoint. The post-plan checkpoint test asserts `runner.stream_states == []` (responder never opened) — a real contract, not weakened.
  - **Session memory** — `test_session_memory.py` still drives `ChatService` through the real `RedisSessionMemory` (over a fake Redis) and asserts the full four-message replay + cap/TTL.
  - **Persistence** — `test_chat_persistence.py` keeps the guest-persists-nothing assertion, the logged-in user+assistant persist (with `message_id`), the cancelled-partial persist, the multi-turn restart-rehydration regression (C1 re-seed), and persist/rehydration failure swallowing.
  - **message_id** — `test_message_id.py` keeps stable-across-events, distinct-per-turn, Redis-JSON round trip, cancelled-partial id, and the never-leaks-to-provider (`to_openai`) assertions.
  - **AuthZ/rate-limit** — `test_chat_api.py`/`test_chat_cancel.py` keep 401 (no auth), 403 (other user's session), 429 path untouched; router unchanged.
- **P4 exit criterion proven end-to-end:** `test_chat_api.py::test_worker_routed_turn_streams_tokens_and_cites_end_to_end` drives the **real** compiled graph (planner → RAG worker → responder) through the actual `POST /api/chat` with only outer collaborators faked, asserting `event: plan`, `"rag"`, `event: token`, and the `rag-source` citation in the `done` frame. This is the genuine contract, not a stub.
- **SSE safety:** event content is JSON-encoded via `json.dumps` in `_format_sse` (no frame injection); the broad `except Exception` (BLE001) does not catch `CancelledError` (BaseException in 3.11), so client-disconnect still cancels; every stream path terminates in exactly one `done`/`cancelled`/`error`.
- **No ReAct/text-parsing reintroduced** (locked v2 decision honored); the deterministic `responder_node`'s placeholder `response` is correctly discarded — the real `Responder` reads `worker_results`/`citations`/`history`, never `state.response` (verified in `responder.py::_build_messages`).
- **Layering respected:** `schemas/chat.py` stays independent of the agent layer (`SourceCitation` is a wire DTO, mapped by the service); the router (`api/chat.py`) is unchanged.

## Verdict: APPROVED
