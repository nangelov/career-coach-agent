# Code review — P1-03-tools · engineer revision 1

## Verdict: APPROVED

## Findings
| id | severity | file:line | issue | required change |
|----|----------|-----------|-------|-----------------|
| C1 | minor | backend/app/tools/internet_search.py:138 | `response.json()` raises `json.JSONDecodeError` (a `ValueError`, **not** an `httpx` error) if a SearXNG instance returns HTTP 200 with a non-JSON body (e.g. an HTML error/landing page). `run()` only catches `httpx.TimeoutException` / `httpx.HTTPError`, so a *direct* `tool.run()` call would leak this exception. The canonical path (`ToolRegistry.execute`) does catch it via its broad `except Exception`, so this is not a loop-crashing bug today — but the tool's own graceful-error contract has a hole. | Catch `ValueError` (or `json.JSONDecodeError`) around `response.json()` in `_search`/`run` and return `ToolResult.error(...)`, so the tool is self-consistently graceful regardless of caller. |
| C2 | nit | backend/app/tools/internet_search.py:129-141 | Untrusted SearXNG JSON is read with no response-body size bound. SearXNG is operator/self-hosted so risk is low, but an unexpectedly large body is fully buffered/parsed. | Optional: bound the read (e.g. cap results at the transport level or check `content-length`). Fine to defer to P10 guardrails; noting only. |

## Notes
- **Acceptance criteria: all met.** Both tools ship a valid OpenAI `tools[]` JSON schema + async executor over one consistent `Tool`/`ToolRegistry` pattern; executors take parsed args and produce a `role="tool"` `ChatMessage` via `ToolResult.to_message` (round trip verified in tests); `internet_search` uses a free/OSS no-key backend (SearXNG) with URL sourced from `app/config.py` (`SEARXNG_URL`), never hard-coded; bad/missing/non-object args and the not-configured case all return graceful error payloads; 19 tool tests pass with `httpx.MockTransport` (no network); `ruff`/`ruff format`/`mypy app/` all clean.
- **Verified locally** (curated env): `ruff check` → All checks passed; `ruff format --check` → 6 files already formatted; `mypy app/` → no issues (25 files); `pytest -q tests/test_tools.py` → 19 passed.
- **Security — clean.** No secrets committed. Search results are reduced to plain `{title, url, snippet}` strings, snippets truncated to 400 chars, result count clamped to `[1,10]`; nothing is executed or surfaced as instructions. No arbitrary-execution surface (no `run_python_code`). The model-supplied `query` reaches `httpx` only via `params=` (properly URL-encoded — no injection). `SEARXNG_URL` is operator config, not user input, so no SSRF-from-untrusted-input concern at this layer.
- **Correctness — solid.** `except httpx.TimeoutException` is correctly ordered before the broader `except httpx.HTTPError` (and `raise_for_status`'s `HTTPStatusError` ⊂ `HTTPError` is caught). `_coerce_max_results` correctly treats `bool` (an `int` subclass) as unspecified. `ToolRegistry.execute`'s broad `except Exception` (BLE001-suppressed with rationale) is the right call for a tool loop and does not swallow `asyncio.CancelledError` (BaseException). Owned vs injected `httpx.AsyncClient` lifecycle (`aclose` only when owned) is handled correctly.
- **Quality — good.** `ToolSchema` is intentionally duplicated locally to keep `tools/` free of any provider-SDK import (consistent with the LLM-layer SDK-isolation decision); documented. Mutable default args (`config = settings`) reference an import-time module singleton — acceptable. Registry rejects duplicate names. Docstrings clearly document the untrusted-data posture and P10 deferral.
- **Scope discipline respected:** no `/api/chat` loop wiring (P1-04), no SerpAPI/job-search (P6), no RAG tools (P2/P4); the "one round trip" example lives in `engineer.md` as required. (The `LLM_*` router config fields also present in `config.py` belong to P1-02, not this task — out of scope here.)
- C1/C2 are minor/nit and do not gate. Recommend the engineer address C1 in a follow-up or fold it into P1-04 wiring, since the registry currently masks it.
