# Code review — P8-08-curated-deps-guard · engineer revision 1

## Verdict: APPROVED

## Findings
| id | severity | file:line | issue | required change |
|----|----------|-----------|-------|-----------------|
| C1 | minor | backend/tests/test_curated_deps.py:62-116 | Negative cases drive `find_problems` in isolation with synthetic sets; there is no full-pipeline negative that mutates a real `pyproject.toml`/curated list and runs `run_check`. Coverage is adequate by composition (parse helpers + `find_problems` + real-file positive), but the end-to-end drift path is not exercised as one unit. | Optional: add one `run_check`-style negative (e.g. tmp copies of the three files with a fake uncurated dep) so the parse→diff→message chain is proven together, not only piecewise. |
| C2 | nit | .github/workflows/backend-ci.yml:88 · backend/Makefile:64 | Guard runs via system `python3` and depends on stdlib `tomllib` (Python ≥3.11). Fine today (ubuntu-latest = 3.12; project floor is 3.11), but the dependency on the *system* interpreter version (not the uv-installed 3.11) is implicit. | Optional: note the 3.11 `tomllib` floor in the CI-step comment / Makefile target so a future runner/base-image downgrade fails loudly rather than as a confusing `ModuleNotFoundError: tomllib`. |

## Notes
- **Allowlist is honestly scoped — verified against source, not taken on trust.** Each `INTENTIONAL_EXCLUSIONS` entry checks out: `redis` is imported at module scope (`app/bootstrap.py:28`, `app/repositories/redis.py:33`) but is genuinely installed transitively by the curated `celery[redis]` extra; `sentence-transformers`, `docling`, `pytesseract`, `PIL`, `ocrmypdf` are all deferred imports (`# noqa: PLC0415 - deferred` inside methods, never hit at pytest collection); `langmem` and `google-search-results`/`serpapi` have no import in `app/` at all (docstring/comment mentions only); `uvicorn` is server-only. The allowlist is anchored externally by P8-07's green CI run on this exact curated list — it codifies a known-good state, it does not paper over a genuinely-missing runtime import.
- **Negative tests are genuine, not tautological.** They call the real production `find_problems` (imported via `importlib` from the actual script), feed a real drift (`newlib` declared, uncurated, not allowlisted), and assert the exact package name plus the actionable "add to backend-ci.yml and Makefile" guidance surfaces. This is the precise shape that would have caught FIX-11 (`reportlab`). The CI↔Makefile parity and stale-allowlist paths are each covered by their own negative. The initial two red tests the engineer reported were a test-setup artifact (synthetic allowlist entry missing from `runtime` tripped the stale check), correctly fixed in the test, not the guard.
- **Parsing is sound for the real files.** `_join_continuations` drops full-line comments before matching so a commented `uv pip install` can't leak; backslash continuations are joined; `normalize` correctly reduces `"celery[redis]>=5.3.0"` → `celery`, matching the pyproject side. `test_real_repo_files_are_clean` exercises the full `run_check` against the committed files and passes.
- **Out-of-scope observation (not a finding against this task):** `joserfc` and `aiosqlite` are curated-in (CI + Makefile) but absent from `pyproject.toml [project].dependencies`. `joserfc` is a real, always-used runtime dep (`app/security/tokens.py`), so a production `uv sync`/`pip install .` would *not* install it — a latent packaging gap. This guard is intentionally one-way (flags declared-but-not-curated only), so it correctly ignores this; flagging here only so it can become a follow-up (add `joserfc` to `pyproject.toml` dependencies). Does not gate P8-08.
- Acceptance criteria all met: package-named failure message; passes on real files with a proven negative; wired as an early CI step before install/pytest; documented in both the script header and the CI comment; full backend suite reported green (739 passed / 62 skipped).
